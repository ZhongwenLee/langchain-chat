app_name: "langchain-chat"
debug: true
storage:
  type: "sqlite"
llm:
  default_model: "gpt-4.1-mini"
environment:
  name: "dev"
  overrides:
    database_profile: "local"
    secret_scope: "developer"
